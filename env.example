# Environment Variables
# Copy this file to .env in the appropriate directories and fill in your actual values
#
# For Server: Copy to server/.env
# For Client: Copy to client/.env

# ============================================
# SERVER ENVIRONMENT VARIABLES
# ============================================
# Copy these to server/.env

# Server Configuration
PORT=5001
NODE_ENV=development

# MongoDB Connection
# Format: mongodb://[username:password@]host[:port][/database][?options]
MONGODB_URI=mongodb://localhost:27017/job-app-assistant
# Example with authentication:
# MONGODB_URI=mongodb://username:password@localhost:27017/job-app-assistant?authSource=admin
# Example with MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/job-app-assistant?retryWrites=true&w=majority

# JWT Authentication
# Generate a secure random string for production (e.g., using: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRY=1d
# Options: 1h, 24h, 7d, 30d, etc.

# GitHub API (for Portfolio Feature)
# Get your token from: https://github.com/settings/tokens
# Click "Generate new token (classic)" and select scopes: public_repo, read:user
# This is used as a fallback if users don't provide their own token
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# ============================================
# CLIENT ENVIRONMENT VARIABLES
# ============================================
# Copy these to client/.env

# API Base URL
# Default: http://localhost:5001/api
# For production, set this to your deployed backend URL
VITE_API_BASE_URL=http://localhost:5001/api
# Example for production:
# VITE_API_BASE_URL=https://api.yourdomain.com/api

